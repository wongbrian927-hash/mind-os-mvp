/** Canonical public origin. Change here if the Vercel project name changes. */
export const PUBLIC_HOST = "mindos.vercel.app";
export const PUBLIC_URL = `https://${PUBLIC_HOST}`;
export const SHARE_URL = `${PUBLIC_URL}/?s=qr`;
